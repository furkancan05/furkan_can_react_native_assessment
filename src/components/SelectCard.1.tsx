import { RickObject } from "@/types/types";
import { Animated } from "react-native";
import { styles } from "./SelectCard";

function SelectCard({ rick }: RickObject) {
  return (
    <Animated.View style={styles.container}>
      <Text></Text>
    </Animated.View>
  );
}
